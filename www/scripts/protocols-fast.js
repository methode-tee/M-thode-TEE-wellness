/* Méthode Tee — optimisation strictement limitée à protocols.html (base 284(3)). */
(function () {
  "use strict";
  if (!document.getElementById("protocolGrid")) return;
  window.mtWaitForRealImagesFromHTML = async function mtProtocolNoBlockingImageWait() {
    return true;
  };
})();
